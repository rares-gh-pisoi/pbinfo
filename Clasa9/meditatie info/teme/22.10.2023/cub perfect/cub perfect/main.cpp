#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n,x;
    cin>>n;
    int r=cbrt(x)
    for (int i=1; i<=n; i++) {
        cin>>x;

        if (r*r*r)
            cout<<"DA"<<"\n";
        else
            cout<<"NU"<<"\n";
    }

    return 0;
}
